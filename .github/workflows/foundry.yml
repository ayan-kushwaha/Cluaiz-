name: 🏛️ Cluaiz Sovereign Foundry

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build-engines:
    name: Build ${{ matrix.os }} Engine
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            suffix: win-x64
            ext: dll
          - os: ubuntu-latest
            suffix: linux-x64
            ext: so
          - os: macos-latest
            suffix: mac-arm64
            ext: dylib

    steps:
      - name: 🛰️ Checkout Code
        uses: actions/checkout@v4

      - name: 🦀 Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc, x86_64-unknown-linux-gnu, aarch64-apple-darwin

      - name: 🛠️ Build Universal Kernels (Llama, Candle, BitNet)
        run: |
          # Build engines with hardware features
          cargo build --release -p engines
        shell: bash

      - name: 📦 Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: engine-${{ matrix.suffix }}
          path: |
            target/release/*.${{ matrix.ext }}
            cluaiz-cure/engines/default_roster.json

  publish-packages:
    name: 🚀 Publish to Packages (GHCR)
    needs: build-engines
    runs-on: ubuntu-latest
    steps:
      - name: 🛰️ Checkout Code
        uses: actions/checkout@v4

      - name: 📥 Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: 🔐 Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏗️ Build & Push Engine Image
        run: |
          # Packaging all binaries (Windows, Linux, Mac) into one OCI Artifact
          echo "FROM scratch" > Dockerfile
          echo "COPY dist/ /" >> Dockerfile
          
          IMAGE_ID=ghcr.io/${{ github.repository_owner }}/cluaiz-engines
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[l-a-z]')
          VERSION=${{ github.ref_name }}
          [ "$VERSION" == "main" ] && VERSION=latest

          docker build -t $IMAGE_ID:$VERSION .
          docker push $IMAGE_ID:$VERSION
        shell: bash
